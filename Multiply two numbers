#include <stdio.h>
int main()
{
    double a,b,product;
printf("Enter two numbers: ");
scanf("%lf %lf", &a,&b);

//calculating product
product= a * b;

//%.2lf didplay number up to 2 decimals
printf("product = %.2lf" , product);
return 0;
}
